# Manual actions for the user — Post-Fable audit remediation (epic SCRUM-404)

The autonomous `/loop` appends to this file whenever a fix has a part only you can do (dashboard toggles, deploys that need a real test call, external account changes). Check this when you're back.

## Known up front (independent of the loop)
- **SCRUM-350** — Supabase dashboard → Authentication → enable leaked-password protection (HaveIBeenPwned). Manual toggle; the loop cannot do it.
- **SCRUM-408 (rank-04, canMakeCall)** & **SCRUM-424 (rank-20, gemini watchdog / recording disclosure)** are `caution-live-path`: the loop merges the code but **defers the `fly deploy`** so you can place a real test call to validate. See per-ticket lines below once processed.

## Appended by the loop as it runs
<!-- loop appends "- [SCRUM-NNN] <action>" lines below this marker -->
- **[VOICE-SERVER DEPLOY — batched]** Several voice-server PRs are merged but **NOT yet deployed** (deferred so a Fly restart doesn't drop live inbound calls). Do ONE `cd voice-server && fly deploy` during a maintenance window, then `curl -fsSL https://phondo-voice.fly.dev/health`. This picks up: **SCRUM-413** (outbound-dialer hardening — safe, the /outbound endpoints stay 404 until you set `OUTBOUND_SMOKE_TEST_ENABLED=true` + `OUTBOUND_ALLOWED_NUMBERS`), plus the `caution-live-path` ones below (**SCRUM-408**, and later SCRUM-414/418/424 as they land) which additionally need their flags set + a real test call. Deploying is otherwise behaviour-neutral for inbound calls.
- **[SCRUM-408] Deploy + activate the subscription gate (deferred — `caution-live-path`).** The code merged but is **dormant** (`ENFORCE_SUBSCRIPTION_GATE` unset = no behaviour change). To activate: (1) `cd voice-server && fly deploy`; (2) `curl -fsSL https://phondo-voice.fly.dev/health` returns ok; (3) set the Fly secret `ENFORCE_SUBSCRIPTION_GATE=true` (start on ONE test org if possible); (4) place a **real inbound call** to a number whose org has an **expired trial** — confirm it routes to forwarding/voicemail and the Loki log `[AnswerMode] subscription-gate block` fires with the org's status; (5) place a call from a **healthy (active/in-trial)** org and confirm AI still answers normally (i.e. `lookupPhoneNumber` still returns a row, not null). Roll back instantly by unsetting the secret if anything misbehaves. Tracking follow-up for full enforcement: SCRUM-434.
- **[SCRUM-412] Enable Supabase Auth CAPTCHA (dashboard).** Auth → Settings → "Bot and Abuse Protection": enable Turnstile or hCaptcha and add the provider site key. Until this is on, signup has no CAPTCHA. After enabling, the captcha-widget plumbing (SCRUM-436) wires the token into `supabase.auth.signUp`.
- **[SCRUM-412] Tighten Supabase Auth rate limits (dashboard).** Auth → Rate Limits: lower the **signup** and **password-reset (email)** limits to bound account enumeration and victim email-bombing (finding #66). Signup and forgot-password go client→Supabase directly (no server route to gate), so their throttle is Supabase-side. The app added a per-IP limit on the trial route + a per-user owned-org cap (migration 00148).
- **[SCRUM-412] Verify "Enable email confirmations" is ON (dashboard).** This is the *authoritative* anti-enumeration control: with it on, Supabase Auth (GoTrue) itself returns an obfuscated success for an already-registered email, so the network response carries no enumeration signal. The signup UI message change is only defense-in-depth on top of it.
- **[SCRUM-412 / SCRUM-350] Leaked-password protection** lives in the same Auth → Settings area — enable it while you're there (HaveIBeenPwned).
- **[SCRUM-408] Confirm lapsed-trial landing status.** During the test call, check whether a no-payment-method expired trial lands on Stripe status `past_due` (which the gate currently ALLOWS as dunning grace) vs `canceled` (blocked). If it's `past_due`, decide via SCRUM-434 whether to also block past_due for never-paid orgs.
- **[SCRUM-422] Delete the `VAPI_TOOL_SECRET` env var from Vercel (and `.env`).** The last three consumers (the dead Vapi-era tool webhook routes `/api/v1/transfer`, `/api/v1/calendar/book-appointment`, `/api/v1/calendar/check-availability`) were deleted — the secret now authorizes nothing. Harmless to leave, but removing it shrinks the secret inventory.
- **[SCRUM-424] Deploy voice-server + place a real test call to verify (deferred — `caution-live-path`).** Code merged; rides the batched `fly deploy` above. After deploying: (1) `curl -fsSL https://phondo-voice.fly.dev/health` ok; (2) place a **real inbound call** to an org with recording disclosure required (AU all-party / consent mode on) — confirm the disclosure is spoken at the start AND the call row has `recordingDisclosurePlayed: true` in metadata; (3) confirm normal calls are unaffected (greeting timing unchanged — the setup watchdog only acts when Gemini setup stalls >10s); (4) optionally tune `GEMINI_SETUP_TIMEOUT_MS` (values <1000 are ignored with a warning). If a call ever ends with `endedReason: gemini-setup-timeout` in the logs, the caller heard a spoken apology before the hangup instead of dead silence.
